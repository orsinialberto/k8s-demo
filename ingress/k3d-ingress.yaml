apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-albe
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: demo-albe
            port:
              number: 80